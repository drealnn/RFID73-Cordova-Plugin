repositories{
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile(name:'DeviceAPI_ver20231208_release', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}