repositories{
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    compile(name:'DeviceAPI20220518', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}